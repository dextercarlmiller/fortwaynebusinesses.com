{
  "name": "fortwaynebusinesses",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "build": "mkdir -p public && cp -r index.html about.html contact.html faq.html css js images _headers _redirects public/",
    "deploy": "npm run build && wrangler deploy"
  }
}
